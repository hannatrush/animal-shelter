{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "timeout 30m nginx -g 'daemon off;'",
    "restartPolicyType": "NEVER"
  }
}
