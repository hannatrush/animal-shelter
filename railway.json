{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "timeout 1m nginx -g 'daemon off;'",
    "restartPolicyType": "ON_FAILURE"
  }
}
